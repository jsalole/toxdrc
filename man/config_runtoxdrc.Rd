% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_runtoxdrc.R
\name{drc_qc}
\alias{drc_qc}
\alias{drc_normalization}
\alias{drc_toxicity}
\alias{drc_modeling}
\title{Configuration helper functions for \code{runtoxdrc()}}
\usage{
drc_qc(
  outlier.test = FALSE,
  cv.flag = TRUE,
  cvflag.lvl = 30,
  pctl.test = FALSE,
  pctl.lvl = 10,
  ref.label = "Control",
  pctl.label = 0,
  avg.resp = TRUE
)

drc_normalization(
  blank.correction = FALSE,
  blank.label = "Blank",
  normalize.resp = FALSE,
  relative.label = 0
)

drc_toxicity(
  toxic.lvl = 0.7,
  toxic.type = c("rel", "abs"),
  toxic.direction = c("below", "above", "different"),
  comp.group = 0,
  target.group = NULL
)

drc_modeling(
  model.list = list(LL.4 = LL.4(), LN.4 = LN.4(), W1.4 = W1.4(), W2.4 = W2.4()),
  model.metric = c("IC", "Res var", "Lack of fit"),
  EDx = 0.5,
  interval = c("tfls", "fls", "delta", "none"),
  level = 0.95,
  type = c("absolute", "relative"),
  EDargs.supplement = list()
)
}
\arguments{
\item{outlier.test}{Logical. Indicates if outliers should be identified.}

\item{cv.flag}{Logical. Indicates if CVs of the response variable should be calculated within each Conc group and flagged (if exceeding \code{cvflag.lvl}).}

\item{cvflag.lvl}{Numeric. The \% at which CVs are considered high.}

\item{pctl.test}{Logical. Indicates if positive control/solvent effects should be evaluated.}

\item{pctl.lvl}{Character. The \% difference in the response \code{ref.label} and \code{pctl.label} at which tests are flagged.}

\item{ref.label}{Character. Label used for the true control level.}

\item{pctl.label}{Character. Label used for the positive control level.}

\item{avg.resp}{Logical. Indicates if responses should be averaged at each Conc level.}

\item{blank.correction}{Logical. Indicates if the response variable should be blank corrected.}

\item{blank.label}{Character. Label used for the blank level.}

\item{normalize.resp}{Logical. Indicates if response variable should be normalized to a given group.}

\item{relative.label}{Character. Label used for the group values will be normalized to.}

\item{toxic.type}{Character. Indicates if \code{toxic.lvl} is an absolute ("abs") value or relative ("rel") to some group.}

\item{toxic.direction}{Character. Indicates if toxicity is indicated by a higher ("above") or lower ("below") response, or  a change in either direction ("different").}

\item{comp.group}{Character. Label used for the group values will be compared to, if \code{toxic.type} is set to relative ("rel").}

\item{target.group}{Character. If NULL, all groups are evaluated to determine toxicity. Giving group name(s) will restrict compairisons to given groups.}

\item{model.list}{A named list of model functions to be fitted. Defaults include
\code{"LL.4"}, \code{"LN.4"}, \code{"W1.4"}, and \code{"W2.4"}.}

\item{model.metric}{Character. Criterion used to select the best model. Choices are
\code{"IC"} (information criterion), \code{"Res var"} (residual variance), or \code{"Lack of fit"}.
Defaults to \code{"IC"}.}

\item{EDx}{Numeric. The effective dose level to estimate (e.g., 0.5 for ED50). Default is 0.5.}

\item{interval}{Character. Method for calculating confidence intervals of EDx. Choices are
\code{"tfls"}, \code{"fls"}, \code{"delta"}, or \code{"none"}. Default is \code{"tfls"}.}

\item{level}{Numeric. Confidence level for the interval calculation. Default is 0.95.}

\item{type}{Character. Whether EDx is calculated as \code{"absolute"} or \code{"relative"}. Default is \code{"absolute"}.}

\item{EDargs.supplement}{List. Optional. User-supplied list of additional or overriding ED arguments.
These will be merged with defaults. Can include elements like \code{interval}, \code{level}, \code{type}, or other arguments from \code{drc::ED()}.}

\item{toxic.lvl.}{Numeric. A cutoff point to determine if modelling occurs. The response variable must Indicates if the response variable should be blank corrected.}
}
\value{
A named list containing the configuration parameters for each helper, suitable for passing to \code{\link[=runtoxdrc]{runtoxdrc()}}.

A list containing quality control settings for use in \code{\link[=runtoxdrc]{runtoxdrc()}}.

A list containing normalization settings for use in \code{\link[=runtoxdrc]{runtoxdrc()}}.

A list containing toxicity determination settings for use in \code{\link[=runtoxdrc]{runtoxdrc()}}.

A list containing model fitting and selection settings for use in \code{\link[=runtoxdrc]{runtoxdrc()}}.
}
\description{
\strong{Quality control configuration}

Defines how quality control is applied to the response data before model fitting.

\strong{Normalization configuration}

Defines how normalization steps are applied to the response data before model fitting.

\strong{Toxicity configuration}

Defines how toxicity is determined before and during model fitting.

\strong{Modelling configuration}

Defines how models are fitted, selected, and point estimates are estimated.
}
\details{
These helper functions provide modular configuration lists for customizing
different stages of the \code{runtoxdrc()} workflow, including data quality control,
normalization, model fitting, and toxicity evaluation.

Each helper returns a named list with default options. Users can modify
only the parameters they need, without manually handling all arguments
in \code{runtoxdrc()}.

\strong{Available configuration functions:}
\itemize{
\item \code{\link[=drc_qc]{drc_qc()}} — Quality control and filtering options
\item \code{\link[=drc_normalization]{drc_normalization()}} — Blank correction and normalization
\item \code{\link[=drc_toxicity]{drc_toxicity()}} — Toxicity threshold and response-level options
\item \code{\link[=drc_modeling]{drc_modeling()}} — Model selection and fitting criteria
}
}
\section{Quality control arguments}{

\describe{
\item{outlier.test}{Logical. Indicates if outliers should be identified. Default: FALSE.}
\item{cv.flag}{Logical. Flag CVs of the response variable. Default: TRUE.}
\item{cvflag.lvl}{Numeric. Percent at which CVs are considered high. Default: 30.}
\item{pctl.test}{Logical. Indicates if positive control/solvent effects should be evaluated. Default: FALSE.}
\item{pctl.lvl}{Numeric. Percent difference in response \code{ref.label} and \code{pctl.label} at which tests are flagged. Default: 10.}
\item{ref.label}{Character. Label used for the true control level. Default: "Control".}
\item{pctl.label}{Character. Label used for the positive control level. Default: 0.}
\item{avg.resp}{Logical. Indicates if responses should be averaged at each concentration. Default: TRUE.}
}
}

\section{Normalization arguments}{

\describe{
\item{blank.correction}{Logical. Indicates if the response variable should be blank corrected. Default: FALSE.}
\item{blank.label}{Character. Label used for the blank level. Default: "Blank".}
\item{normalize.resp}{Logical. Indicates if response variable should be normalized to a given group. Default: FALSE.}
\item{relative.label}{Character/Numeric. Label or value used for the group values will be normalized to. Default: 0.}
}
}

\section{Toxicity arguments}{

\describe{
\item{toxic.lvl}{Numeric. Cutoff point to determine if modelling occurs. Default: 0.7.}
\item{toxic.type}{Character. Indicates if \code{toxic.lvl} is absolute ("abs") or relative ("rel") to some group. Default: "rel".}
\item{toxic.direction}{Character. Indicates if toxicity is a higher ("above"), lower ("below"), or any change ("different"). Default: "below".}
\item{comp.group}{Character/Numeric. Label used for the group values will be compared to if \code{toxic.type} is "rel". Default: 0.}
\item{target.group}{Character/Numeric. Optional. Restrict comparisons to given group(s). Default: NULL.}
}
}

\section{Modelling arguments}{

\describe{
\item{model.list}{Named list of model functions to be fitted. Defaults: \code{"LL.4"}, \code{"LN.4"}, \code{"W1.4"}, \code{"W2.4"}.}
\item{model.metric}{Character. Criterion used to select best model. Choices: \code{"IC"}, \code{"Res var"}, \code{"Lack of fit"}. Default: "IC".}
\item{EDx}{Numeric. Effective dose level to estimate (e.g., 0.5 for ED50). Default: 0.5.}
\item{interval}{Character. Method for calculating confidence intervals of EDx. Choices: \code{"tfls"}, \code{"fls"}, \code{"delta"}, \code{"none"}. Default: "tfls".}
\item{level}{Numeric. Confidence level for the interval calculation. Default: 0.95.}
\item{type}{Character. Whether EDx is calculated as \code{"absolute"} or \code{"relative"}. Default: "absolute".}
\item{EDargs.supplement}{List. Optional user-supplied list of additional or overriding ED arguments. Can include \code{interval}, \code{level}, \code{type}, or other arguments compatible with \code{drc::ED()}.}
}
}

\examples{
# Default QC configuration
drc_qc()

# Custom QC configuration
drc_qc(outlier.test = TRUE, cvflag.lvl = 20)

# Default normalization
drc_normalization()

# Default toxicity configuration
drc_toxicity()

# Default modelling configuration
drc_modeling()

# Override some ED arguments
drc_modeling(EDargs.supplement = list(interval = "delta", level = 0.9))

}
\seealso{
\code{\link[=runtoxdrc]{runtoxdrc()}}, \code{\link[drc:ED.drc]{drc::ED()}}

\code{\link[drc:ED.drc]{drc::ED()}}
}
